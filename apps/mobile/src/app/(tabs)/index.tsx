import { InfoScreen } from '@pkg/ui/features';

import { VersionInfo } from '~/components/VersionInfo';

export default () => {
  return (
    <InfoScreen>
      <VersionInfo />
    </InfoScreen>
  );
};
